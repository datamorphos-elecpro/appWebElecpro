import { Page } from '../../../components/ui/Page'; import { CatalogManager } from '../../../components/business/CatalogManager'; import { getRows } from '../../../lib/data'; export default async function Catalog(){return <Page title="Productos y servicios" description="Catálogo de materiales y mano de obra."><CatalogManager kind="catalog_items" rows={await getRows('catalog_items','id,code,description,unit,base_unit_price,category,is_active')}/></Page>}
