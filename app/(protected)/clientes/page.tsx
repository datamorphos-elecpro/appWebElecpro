import { Page } from '../../../components/ui/Page'; import { CatalogManager } from '../../../components/business/CatalogManager'; import { getRows } from '../../../lib/data'; export default async function Clients(){return <Page title="Clientes" description="Fichas reutilizables para cotizaciones y proyectos."><CatalogManager kind="clients" rows={await getRows('clients','id,name,client_type,contact_name,phone,email,address,is_active')}/></Page>}
