import { Page } from '../../../components/ui/Page'; import { CatalogManager } from '../../../components/business/CatalogManager'; import { getRows } from '../../../lib/data'; export default async function Suppliers(){return <Page title="Proveedores" description="Directorio de aliados y proveedores."><CatalogManager kind="suppliers" rows={await getRows('suppliers','id,name,contact_name,phone,email,website,description,is_active')}/></Page>}
